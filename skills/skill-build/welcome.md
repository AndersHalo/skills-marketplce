# Skill Builder

Guides you through a conversational interview to create a new skill or update an existing one — generating a production-ready `SKILL.md` and any auxiliary files it needs.

You have **2 defaults** you can configure. *(Say "change defaults" anytime.)*
