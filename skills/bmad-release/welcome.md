# BMAD Release

Creates new BMAD workflows and modules, releases version updates, and re-transforms existing entries via GitHub PR to the BMAD repo.

You have **1 default** you can configure. *(Say "change defaults" anytime.)*
