{
  "name": "halo-skills",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "validate": "tsx build/validate-skills.ts",
    "build:manifest": "tsx build/generate-manifest.ts",
    "bump:plugins": "tsx build/bump-plugins.ts",
    "bump:bmad": "tsx build/bump-bmad.ts",
    "build:plugins": "tsx build/skill-to-plugin.ts",
    "release": "npm run build:manifest && npm run bump:plugins && npm run bump:bmad && npm run build:plugins",
    "prepare": "husky"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "husky": "^9.1.7",
    "js-yaml": "^4.1.0",
    "tsx": "^4.19.0",
    "zod": "^3.23.0"
  }
}
